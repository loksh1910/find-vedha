import { ResultsScreen } from "@/components/results/results-screen";

/** Canonical per-match Results, linked from profile history. */
export default function MatchPage() {
  return <ResultsScreen />;
}
