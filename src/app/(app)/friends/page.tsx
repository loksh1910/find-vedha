import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export default function FriendsPage() {
  return (
    <PlaceholderScreen title="Friends" phase="Phase 7">
      Friend requests, online status, recent players and invite-from-lobby land
      here once the social layer is wired up. For now the dashboard shows a mock
      friends list.
    </PlaceholderScreen>
  );
}
