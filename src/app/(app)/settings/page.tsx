import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export default function SettingsPage() {
  return (
    <PlaceholderScreen title="Settings" phase="Phase 8">
      Audio, board contrast, motion density and account controls will live here.
      The app is dark-mode only — there is no light theme to switch to.
    </PlaceholderScreen>
  );
}
